TASK: Recommend 3 alternative statistical analysis approaches for the study.

OUTPUT FORMAT — MANDATORY:
Present EXACTLY 3 alternative analysis approaches. The user will pick ONE.

Format your response as:

1. [Test name — e.g., "Independent samples t-test"]. [1-2 sentences explaining what this tests and why it fits the data.] Assumptions: [list key assumptions]. Effect size: [d, η², r, etc.]. Data needed: [brief description of data structure required].

2. [Test name — alternative, e.g., "Mann-Whitney U (non-parametric)"]. [1-2 sentences explaining what this tests and when to prefer it.] Assumptions: [list key assumptions]. Effect size: [r, rank-biserial]. Data needed: [data structure].

3. [Test name — third alternative]. [1-2 sentences.] Assumptions: [list]. Effect size: [measure]. Data needed: [structure].

Do NOT include any text outside the numbered options.

GUIDELINES:
- The 3 options should represent meaningful alternatives: parametric vs non-parametric, OR different test types, OR different effect size choices
- All 3 must be applicable to the study's data structure
- Statistical significance threshold: α = .05

TEST FAMILIES TO CHOOSE FROM:
- Comparing groups on continuous DV: t-test (Cohen's d), ANOVA (η²), ANCOVA (partial η²), Mann-Whitney U (r), Kruskal-Wallis (η²H)
- Examining relationships: Pearson r, Spearman ρ, Multiple regression (R²)
- Categorical: Chi-square (Cramér's V), Fisher's exact (OR)
- Bayesian alternatives: Bayes factor t-test, Bayesian correlation

EXAMPLE OPTION:
1. Independent samples t-test. Tests whether mean GAD-7 scores differ between the mindfulness training group and the control group. Assumptions: normality of DV within groups (Shapiro-Wilk), homogeneity of variance (Levene's), independence of observations. Effect size: Cohen's d. Data needed: two arrays of numerical scores, one per group.
